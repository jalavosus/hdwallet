name: Lint

on:
  pull_request:
  push:
    branches:
      - '*'

jobs:
  lint:
    uses: jalavosus/workflows/.github/workflows/go-lint-117_compat.yml@v1.2.0
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}